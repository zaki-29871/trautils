from setuptools import setup

setup(name='trautils',
      version='1.0.0',
      description='Some utils',
      packages=['trautils'],
      install_requires=['colorama', 'numpy', 'tqdm'],
      zip_safe=False)